# DocAlign v0.1 隐私说明

DocAlign v0.1 是本地单用户工具。API 与工作台默认只监听 `127.0.0.1`，不包含账户、遥测、
云存储或公共部署逻辑。

## 本地数据

- 上传 DOCX、分析 IR、临时规则、独立规则包、任务、输出和审计保存在 `DOCALIGN_DATA_DIR`。
- 源文件不会被覆盖；输出通过重开与内容安全验证后才原子发布。
- 工作台只在浏览器 `localStorage` 保存当前文档、分析和任务的随机 ID，以便刷新恢复。
- 用户点击“删除本地文档”后，服务级联删除该文档的分析、临时规则、任务和产物；“我的规则包”
  用于跨文档复用，独立保留，不会随单份文档删除。
- 规则包修订包含名称、适用范围、说明、核对记录、变更说明、规则正文和 SHA-256，不包含待处理
  文档正文；导出文件由用户自行保管。
- DocAlign 默认保留 1 GiB 安全可写余量，并在上传、排版、打包或备份前做容量预检。该判断不会
  锁定磁盘、自动扩容或自动删除资料；其他程序仍可能在预检后占用空间。

## 完整工作区备份

- 用户从设置页主动下载完整备份时，包内会包含全部托管源 DOCX、原文件名、分析、规则历史、任务、
  批次、审计、输出和数据库快照；这是敏感数据副本，不会自动上传。
- `.env`、模型密钥、运行锁、进程元数据、SQLite WAL/SHM 和数据目录中的非托管根文件不会进入包。
- 备份使用 SHA-256 和严格清单发现损坏或错配，但当前不加密、不使用发布者数字签名。用户应按原始
  文档的保密等级存储和传输，并在恢复前使用内置校验器。
- 恢复命令只写入一个尚不存在的新目录，不覆盖当前工作区；恢复完成后由用户明确配置并启动该目录。

## 可选模型

- 模型密钥仅从 API 进程环境读取，不进入浏览器、SQLite 或审计日志。
- 自然语言规则编译只发送格式要求和段落数、角色统计、文档类型、样式名等紧凑摘要。
- “智能分析”默认不会自动运行。用户确认后，会发送逐段文字、段落顺序、样式名、编号与
  紧凑格式证据，用于判断标题、正文、作者、列表等语义角色。
- DOCX 文件本身、图片二进制、关系、书签、字段、完整运行 XML 和 OOXML 永不发送给模型。
- `DOCALIGN_LLM_BASE_URL` 指向的远端服务适用其自身隐私政策；不配置模型时结构化流程仍完整可用。

## 文档内容

审计报告记录哈希、计数、角色、操作、假设、警告和验证结果，不记录完整正文。

## 支持诊断

- 本机诊断只在用户点击“运行诊断”或主动执行离线命令时生成，不在后台运行，也不自动上传。
- 报告包含 DocAlign/Python/操作系统版本、配置是否完整、数据库和磁盘汇总、检查结论、处理建议
  以及近 30 天错误代码计数。
- 报告明确排除正文、文件名、文档/分析/任务等记录 ID、完整本地路径、数据库连接串、模型端点、
  密钥和原始日志；异常原文不会进入报告，非标准错误代码会归并为 `UNKNOWN_ERROR`。
- 诊断遇到数据目录外的产物引用时只标记为无效，不读取该外部文件。离线诊断不会创建缺失的数据库
  或数据目录，也不会执行迁移。
- 下载的诊断 JSON 由用户自行检查、保管并决定是否分享；DocAlign 当前没有诊断上传或遥测服务。
